
core=$1

echo loading core $core

curl -q "http://localhost:9077/search/admin/cores?wt=json&action=RELOAD&core=$core"

cat $core.tab.headers $core.tab  | curl -q http://localhost:9077/search/$core/update?rowid=document_id\&wt=json\&separator=%09 --data-binary @- -H 'Content-type:application/csv; charset=utf-8'

curl -q "http://localhost:9077/search/admin/cores?wt=json&action=RELOAD&core=$core"
