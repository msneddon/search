[uwsgi]
socket = uwsgi.sock

http = 0.0.0.0:7078

master = true
processes = 4
threads = 2

pidfile = service.pid
daemonize = search_uwsgi.log

wsgi-file = search/search_wsgi.py
callable = search_wsgi
